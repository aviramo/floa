{
  "name": "floa",
  "private": true,
  "type": "module",
  "description": "FLOA — a component-built static site. Pages are assembled from src/components by build.mjs.",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --serve",
    "watch": "node build.mjs --watch"
  }
}
